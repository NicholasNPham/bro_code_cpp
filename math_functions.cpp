#include <iostream>
#include <cmath>

int main()
{

    double x = 3.99;
    double y = 4;
    double z;

    // References can be found here: https://www.cplusplus.com/reference/cmath/

    // z = std::max(x, y);
    // z = std::min(x, y);
    // z = pow(2, 3);
    // z = sqrt(9);
    // z = abs(-3);
    // z = round(x);
    // z = ceil(x); // rounds up 
    // z = floor(x); // rounds down

    std::cout << z << "\n";

    return 0;
}